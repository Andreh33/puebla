/**
 * Re-export simplificado de sonner para usar toasts en componentes cliente.
 * Uso:
 *   import { toast } from "@/hooks/use-toast";
 *   toast.success("Producto guardado");
 *   toast.error("No se pudo guardar");
 */
export { toast } from "sonner";
